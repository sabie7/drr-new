# 🚀 ترحيل النسخة إلى أي سيرفر — دليل خطوة بخطوة

> **القاعدة الذهبية:** قاعدة البيانات على MongoDB Atlas (سحابية خارجية) —
> **لا تنتقل أبداً بين السيرفرات**. ما ينتقل فقط: الكود (من جيت هب) + وسائط الرفع.

## المتطلبات على السيرفر الجديد
- Node.js 20+ · npm · Git
- (اختياري) Docker إن أردت التشغيل بالحاويات

## الخطوات

### 1) الكود
```bash
git clone https://github.com/sabie7/drr-new.git app && cd app
npm install                # جذر المشروع
npm run build              # سيرفر + حزمة الدردشة
cd cp-app && npm install && npm run build && cd ..   # لوحة التحكم
```

### 2) البيئة
```bash
cp .env.example .env
# عدّل MONGO_URI بنفس كلاستر Atlas الحالي → كل البيانات تظهر فوراً
# عدّل PRIMARY_HOST لدومين السيرفر الجديد
```

### 3) الوسائط (مرة واحدة فقط عند أول ترحيل)
انسخ مجلد `client/uploads/site/` من السيرفر القديم إلى الجديد:
```bash
scp -r old-host:app/client/uploads new-host:app/client/
# أو عبر أي أداة نقل تفضّلها — المجلد واحد ولا يتغير مساره
```

### 4) التشغيل
```bash
npm start                  # يشغّل dist-server/server.js
# أو بالحاويات:
docker compose up -d
```

### 5) الدومين
وجّه DNS للسيرفر الجديد، وحدّث `PRIMARY_HOST` في `.env` ثم أعد التشغيل.

---

## ✅ قائمة تحقق ما بعد التشغيل
- [ ] فتح الموقع: الصفحة الرئيسية + الدخول كزائر يعملان
- [ ] إرسال رسالة عامة تظهر لحظياً
- [ ] فتح `/cp` وتسجيل دخول الأدمن والتحقق من بطاقة الحدود الجديدة
- [ ] رفع صورة من اللوحة تظهر في الموقع
- [ ] الأصوات/الوسائط تعمل (ملفات uploads منسوخة)

---
⚠️ **تذكير bonto:** أي رفع ملفات هناك يتطلب `restart --hard` يدوياً بعد الرفع.
